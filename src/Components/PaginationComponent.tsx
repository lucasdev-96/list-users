import Pagination from 'react-bootstrap/Pagination';

function PaginationComponent(users: any) {
    // const items = [];
    // for (let number = 1; number <= users.value?.info.page; number++) {
    //   items.push(
    //     <Pagination.Item key={number} >
    //       {number}
    //     </Pagination.Item>,
    //   );
    // }
    
  return (
    <div>ok</div>
  );
}

export default PaginationComponent;